name: CI Build
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
# torch-scatter publishes no PyPI wheels, and its sdist imports torch at
# build time with no build isolation, so a source build fails with
# "No module named 'torch'". Point pip at the PyG wheel index so every job
# installs the prebuilt wheel instead of building. (find-links under
# [tool.uv] in pyproject.toml is honoured only by uv, not by pip.)
env:
  PIP_FIND_LINKS: "https://data.pyg.org/whl/torch-2.12.0+cpu.html"

jobs:
  pre-commit-run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: pip install -e ".[dev]"
      - name: Run pre-commit
        run: pre-commit run --verbose  --all-files

  security:
      runs-on: ubuntu-latest
      container: python:3.12-slim

      steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          # setuptools>=83.0.0 clears PYSEC-2026-3447
          python -m pip install --upgrade pip wheel "setuptools>=83.0.0"
          pip install -e ".[dev]"

      - name: Security Checks
        run: |
          bandit --severity-level high .

  pytests:
      runs-on: ubuntu-latest
      container: python:3.12-slim

      steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          # setuptools>=83.0.0 clears PYSEC-2026-3447
          python -m pip install --upgrade pip wheel "setuptools>=83.0.0"
          # Pin torch to the newest release with prebuilt torch-scatter wheels on
          # the PyG index (currently 2.12.x). Newer torch (e.g. 2.13) has no
          # torch-scatter wheel, so pip would fall back to building from sdist,
          # which fails under build isolation (import torch) with no compiler in
          # the slim image.
          pip install -e ".[test]" "torch==2.12.*"
          TORCH_VERSION=$(python -c "import torch; print(torch.__version__.split('+')[0])")
          pip install torch-scatter -f "https://data.pyg.org/whl/torch-${TORCH_VERSION}+cpu.html"

      - name: Unit tests
        env:
          MLFLOW_ALLOW_FILE_STORE: "true"
        run: |
          pytest --cov=. tests/

  detect_secrets:
    name: IBM detect-secrets
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install prerequisites
        run: sudo apt-get update && sudo apt-get install -y jq diffutils
      - name: Install IBM detect-secrets

        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install --upgrade "git+https://github.com/ibm/detect-secrets.git@master#egg=detect-secrets"
          python3 -m pip install boxsdk

      - name: Scan repository & write snapshot
        run: |
          mkdir -p security-outputs

          # Run detect-secrets while skipping binary files
          detect-secrets scan \
            --exclude-files '.*\.ipynb$|.*\.(png|jpg|jpeg|gif|pdf|onnx|pt|pth|bin|zip)$' \
            --exclude-lines '"(outputs|image/\w+|id|hash)":.*' \
            --force-use-all-plugins \
            . \
            > security-outputs/ds-results.json \
            2> security-outputs/.secrets.new || true


      - name: Upload scan artifacts (for triage)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: detect-secrets-artifacts
          path: .secrets.new

  codeql:
    name: CodeQL (Python)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: github/codeql-action/init@v3
        with:
          languages: python
      - uses: github/codeql-action/autobuild@v3
      - uses: github/codeql-action/analyze@v3
        with:
          category: "/language:python"


  #semgrep:
  #  name: Semgrep (p/default + local overrides)
  #  runs-on: ubuntu-latest
  #  steps:
  #    - uses: actions/checkout@v4

  #    - name: Install Semgrep
  #      run: pip install semgrep


  #    - name: Semgrep scan (PR diff-aware)
  #      if: ${{ github.event_name == 'pull_request' }}
  #      run: |
  #        semgrep \
  #          --config=p/default \
  #          --config=.semgrep.yaml \
  #          --error \
  #          --baseline-commit "${{ github.event.pull_request.base.sha }}" \
  #          .

  #    - name: Semgrep scan (full)
  #      if: ${{ github.event_name != 'pull_request' }}
  #      run: |
  #        semgrep \
  #          --config=p/default \
  #          --config=.semgrep.yaml \
  #          --error \


  bandit:
    name: Bandit (Python)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install Bandit
        run: pip install bandit
      - name: Run Bandit
        run: bandit -ll -ii -r gridfm-graphkit -f json -o bandit-report.json

      - name: Upload artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bandit-report
          path: bandit-report.json

  pip_audit:
    name: pip-audit (deps)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install package (editable) and dev/test extras if present
        run: |
          # NOTE: torch<2.13 pins setuptools<82, so a setuptools>=83 upgrade is
          # downgraded back to 81 at install time -> PYSEC-2026-3447 cannot be
          # resolved by upgrading and is ignored below (build-time sdist issue,
          # macOS-only NFC/NFD MANIFEST.in bypass; not reachable in CI/runtime).
          python -m pip install --upgrade pip
          pip install -e .[dev,test] || pip install -e .
      - name: Run pip-audit
        uses: pypa/gh-action-pip-audit@v1.1.0
        with:
          # Skip audit check on the private/un-indexed module
          skip-requirement: gridfm-graphkit
          # CVE-2026-4539: pygments AdlLexer ReDoS, local-only attack vector, no fix released yet
          # PYSEC-2025-206, PYSEC-2025-204, PYSEC-2025-203: torch 2.8.0 issues, fixed in 2.9.0
          # PYSEC-2026-139: torch pt2 deserialization, local-only, no fix released yet
          # CVE-2025-2999, CVE-2025-3000, CVE-2025-3001: torch 2.8.0 memory corruption bugs (local execution vector)
          # CVE-2026-69247: cryptography PKCS#7 EnvelopedData Bleichenbacher oracle; not reachable, this repo never does PKCS#7/S-MIME decryption
          # PYSEC-2026-3624: lightning 2.6.5 RCE in _load_state via crafted checkpoint _instantiator hyperparameters (load_from_checkpoint). Only exploitable when loading UNTRUSTED checkpoints; our train/predict flows load our own. Temporary ignore (added 2026-08-17) to unblock all PRs; tracked in #103, to be replaced by the proper lightning upgrade (option 1) soon.
          ignore-vulns: |
            CVE-2026-4539
            PYSEC-2025-206
            PYSEC-2025-204
            PYSEC-2025-203
            PYSEC-2026-139
            CVE-2025-2999
            CVE-2025-3000
            CVE-2025-3001
            CVE-2026-69247
            PYSEC-2026-3624
            PYSEC-2026-3447

  trivy_repo:
      name: Trivy (repo scan)
      runs-on: ubuntu-latest
      permissions:
        security-events: write
      steps:
        - uses: actions/checkout@v4

        - name: Run Trivy vulnerability scanner in repo mode
          # We use the official container-based action to avoid binary install issues
          uses: aquasecurity/trivy-action@master
          with:
            scan-type: 'fs'
            ignore-unfixed: true
            format: 'sarif'
            output: 'trivy-results.sarif'
            severity: 'HIGH,CRITICAL'

        - name: Upload SARIF to Code Scanning
          uses: github/codeql-action/upload-sarif@v3
          if: always() # Upload results even if vulnerabilities are found
          with:
            sarif_file: 'trivy-results.sarif'
